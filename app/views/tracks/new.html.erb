<h1>Track#new</h1>

<%= form_for @track do |f| %>
  <%= f.label "Title" %>
  <%= f.text_field :title %>
  <%= f.label "Content" %>
  <%= f.text_area :content %>
  <%= f.collection_check_boxes :lesson_ids, Lesson.all, :id, :name %>
  <%= f.submit %>
<% end %>
